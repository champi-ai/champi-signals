{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"champi-signals","text":"<p>Signal management and event processing library for Champi voice assistant services.</p>"},{"location":"#overview","title":"Overview","text":"<p><code>champi-signals</code> provides signal management infrastructure used across the Champi service ecosystem:</p> <ul> <li><code>BaseSignalManager</code> \u2014 base class for building singleton signal managers</li> <li><code>SignalBridgeABC</code> \u2014 abstract base for cross-process signal bridge implementations</li> <li><code>EventProcessor</code> \u2014 decorator for registering typed event handler functions</li> <li><code>STTEventTypes</code> / <code>TTSEventTypes</code> \u2014 built-in string-value event type enums</li> <li><code>ImgUIEventTypes</code> \u2014 integer event codes for the ImgUI rendering service</li> <li><code>EnumSetup</code> \u2014 utility class for creating string-value enum registries</li> <li><code>make_event_types</code> \u2014 factory for generating integer-keyed event enums from a spec dict</li> </ul>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install champi-signals\n</code></pre> <p>For development:</p> <pre><code>git clone https://github.com/champi-ai/champi-signals.git\ncd champi-signals\nuv sync --extra dev\n</code></pre>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>from champi_signals import (\n    BaseSignalManager,\n    STTEventTypes,\n    ImgUIEventTypes,\n    make_event_types,\n)\n\n# Use a pre-defined IntEnum for ImgUI events\nprint(ImgUIEventTypes.TOOL_CALL_START)  # 100\n\n# Generate a custom IntEnum from a spec\nMyEvents = make_event_types(\"MyEvents\", {\n    \"PROCESS\": [\"START\", \"FINISH\", \"ERROR\"],\n    \"RENDER\":  [\"BEGIN\", \"END\"],\n})\nprint(MyEvents.PROCESS_START)  # 1\n\n# Build a signal manager wired to enum members\nclass MyManager(BaseSignalManager):\n    def __init__(self):\n        super().__init__()\n        self.setup_custom_signals({\"stt\": STTEventTypes})\n\nmanager = MyManager()\nmanager.stt.connect(lambda sender, **kw: print(\"signal:\", kw))\n</code></pre>"},{"location":"changelog/","title":"Changelog","text":"<p>The full changelog is maintained in CHANGELOG.md at the repository root.</p> <p>For the current release history see CHANGELOG.md.</p>"},{"location":"integration/","title":"Integration Guide","text":"<p><code>champi-signals</code> is the shared signal bus used by all Champi services.  Each service creates a typed signal manager by subclassing <code>BaseSignalManager</code> and wiring it to an enum that describes that service's event vocabulary.</p>"},{"location":"integration/#champi-stt","title":"champi-stt","text":"<p>The speech-to-text service uses <code>STTEventTypes</code> to broadcast lifecycle, model, processing, and telemetry events over its signal manager.</p> <pre><code>from champi_signals import BaseSignalManager, STTEventTypes\n\nclass STTSignalManager(BaseSignalManager):\n    def __init__(self):\n        super().__init__()\n        self.setup_custom_signals({\n            \"lifecycle\":  STTEventTypes,\n            \"model\":      STTEventTypes,\n            \"processing\": STTEventTypes,\n            \"telemetry\":  STTEventTypes,\n        })\n</code></pre>"},{"location":"integration/#champi-tts","title":"champi-tts","text":"<p>The text-to-speech service mirrors the same pattern with <code>TTSEventTypes</code>.</p> <pre><code>from champi_signals import BaseSignalManager, TTSEventTypes\n\nclass TTSSignalManager(BaseSignalManager):\n    def __init__(self):\n        super().__init__()\n        self.setup_custom_signals({\n            \"lifecycle\":  TTSEventTypes,\n            \"processing\": TTSEventTypes,\n        })\n</code></pre>"},{"location":"integration/#champi-imgui","title":"champi-imgui","text":"<p>The ImgUI rendering service uses <code>ImgUIEventTypes</code> (an <code>IntEnum</code>) so that event codes can be compared numerically and serialised as plain integers over IPC.</p> <pre><code>from champi_signals import BaseSignalManager, ImgUIEventTypes\n\nclass ImgUISignalManager(BaseSignalManager):\n    def __init__(self):\n        super().__init__()\n        self.setup_custom_signals({\"imgui\": ImgUIEventTypes})\n\nmanager = ImgUISignalManager()\nmanager.imgui.send(\"canvas\", event=ImgUIEventTypes.CANVAS_UPDATE)\n</code></pre>"},{"location":"integration/#champi-ipc","title":"champi-ipc","text":"<p>The IPC layer uses <code>make_event_types</code> to generate a custom <code>IntEnum</code> from a configuration dict, keeping the event vocabulary co-located with the IPC channel definition:</p> <pre><code>from champi_signals import make_event_types\n\nIPCEvents = make_event_types(\"IPCEvents\", {\n    \"CHANNEL\": [\"OPEN\", \"CLOSE\", \"ERROR\"],\n    \"MESSAGE\": [\"SEND\", \"RECEIVE\", \"DROP\"],\n})\n</code></pre>"},{"location":"api/bridges/","title":"bridges","text":"<p>Abstract base class for cross-process signal bridge implementations.</p> <p>Signal bridge abstract base class for connecting blinker signals to external transports.</p>"},{"location":"api/bridges/#champi_signals.bridges.SignalBridgeABC","title":"<code>SignalBridgeABC</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for bridging blinker signals to external transports.</p> <p>Subclasses implement the transport-specific connect/disconnect and async start/stop lifecycle, enabling champi-ipc and other consumers to bind a <code>BaseSignalManager</code> to any message-passing backend.</p> Source code in <code>src/champi_signals/bridges.py</code> <pre><code>class SignalBridgeABC(ABC):\n    \"\"\"Abstract base class for bridging blinker signals to external transports.\n\n    Subclasses implement the transport-specific connect/disconnect and\n    async start/stop lifecycle, enabling champi-ipc and other consumers\n    to bind a ``BaseSignalManager`` to any message-passing backend.\n    \"\"\"\n\n    _manager: BaseSignalManager | None = None\n\n    @abstractmethod\n    def connect(self, signal_manager: BaseSignalManager) -&gt; None:\n        \"\"\"Bind this bridge to a signal manager.\n\n        Args:\n            signal_manager: The ``BaseSignalManager`` instance whose signals\n                            should be forwarded over the transport.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def disconnect(self) -&gt; None:\n        \"\"\"Unbind this bridge from its signal manager.\"\"\"\n        ...\n\n    @abstractmethod\n    async def start(self) -&gt; None:\n        \"\"\"Start the underlying transport (open sockets, threads, etc.).\"\"\"\n        ...\n\n    @abstractmethod\n    async def stop(self) -&gt; None:\n        \"\"\"Stop the underlying transport and release resources.\"\"\"\n        ...\n\n    @property\n    def is_connected(self) -&gt; bool:\n        \"\"\"Return ``True`` if a signal manager is currently bound.\"\"\"\n        return self._manager is not None\n</code></pre>"},{"location":"api/bridges/#champi_signals.bridges.SignalBridgeABC.is_connected","title":"<code>is_connected</code>  <code>property</code>","text":"<p>Return <code>True</code> if a signal manager is currently bound.</p>"},{"location":"api/bridges/#champi_signals.bridges.SignalBridgeABC.connect","title":"<code>connect(signal_manager)</code>  <code>abstractmethod</code>","text":"<p>Bind this bridge to a signal manager.</p> <p>Parameters:</p> Name Type Description Default <code>signal_manager</code> <code>BaseSignalManager</code> <p>The <code>BaseSignalManager</code> instance whose signals             should be forwarded over the transport.</p> required Source code in <code>src/champi_signals/bridges.py</code> <pre><code>@abstractmethod\ndef connect(self, signal_manager: BaseSignalManager) -&gt; None:\n    \"\"\"Bind this bridge to a signal manager.\n\n    Args:\n        signal_manager: The ``BaseSignalManager`` instance whose signals\n                        should be forwarded over the transport.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/bridges/#champi_signals.bridges.SignalBridgeABC.disconnect","title":"<code>disconnect()</code>  <code>abstractmethod</code>","text":"<p>Unbind this bridge from its signal manager.</p> Source code in <code>src/champi_signals/bridges.py</code> <pre><code>@abstractmethod\ndef disconnect(self) -&gt; None:\n    \"\"\"Unbind this bridge from its signal manager.\"\"\"\n    ...\n</code></pre>"},{"location":"api/bridges/#champi_signals.bridges.SignalBridgeABC.start","title":"<code>start()</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Start the underlying transport (open sockets, threads, etc.).</p> Source code in <code>src/champi_signals/bridges.py</code> <pre><code>@abstractmethod\nasync def start(self) -&gt; None:\n    \"\"\"Start the underlying transport (open sockets, threads, etc.).\"\"\"\n    ...\n</code></pre>"},{"location":"api/bridges/#champi_signals.bridges.SignalBridgeABC.stop","title":"<code>stop()</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Stop the underlying transport and release resources.</p> Source code in <code>src/champi_signals/bridges.py</code> <pre><code>@abstractmethod\nasync def stop(self) -&gt; None:\n    \"\"\"Stop the underlying transport and release resources.\"\"\"\n    ...\n</code></pre>"},{"location":"api/enums/","title":"enums","text":"<p>Built-in event type enums and factory utilities.</p> <p>Enums extracted from Champi services with setup methods.</p>"},{"location":"api/enums/#champi_signals.enums.EnumSetup","title":"<code>EnumSetup</code>","text":"<p>Utility class for setting up custom service enums.</p> Source code in <code>src/champi_signals/enums.py</code> <pre><code>class EnumSetup:\n    \"\"\"Utility class for setting up custom service enums.\"\"\"\n\n    @staticmethod\n    def create_event_types(\n        service_name: str, event_categories: list[str] = None\n    ) -&gt; type[Enum]:\n        \"\"\"\n        Create a custom EventTypes enum for a service.\n\n        Args:\n            service_name: Name of the service (e.g., 'NLP', 'AUDIO')\n            event_categories: List of event categories. Defaults to standard categories.\n\n        Returns:\n            Enum class with event types\n\n        Example:\n            NLPEventTypes = EnumSetup.create_event_types(\n                'NLP', ['lifecycle', 'processing', 'analysis']\n            )\n        \"\"\"\n        if event_categories is None:\n            event_categories = [\"lifecycle\", \"model\", \"processing\", \"telemetry\"]\n\n        # Create enum attributes\n        enum_attrs = {}\n        for category in event_categories:\n            enum_name = f\"{category.upper()}_EVENT\"\n            enum_value = f\"{category.lower()}_event\"\n            enum_attrs[enum_name] = enum_value\n\n        # Create the enum class dynamically\n        return Enum(f\"{service_name}EventTypes\", enum_attrs)\n\n    @staticmethod\n    def create_lifecycle_events(\n        service_name: str, custom_events: list[str] = None\n    ) -&gt; type[Enum]:\n        \"\"\"\n        Create a custom LifecycleEvents enum for a service.\n\n        Args:\n            service_name: Name of the service\n        custom_events: Custom lifecycle events. If None, uses standard events.\n\n        Returns:\n            Enum class with lifecycle events\n        \"\"\"\n        if custom_events is None:\n            custom_events = [\n                \"uninitialized\",\n                \"initializing\",\n                \"ready\",\n                \"processing\",\n                \"error\",\n                \"shutdown\",\n                \"stopped\",\n                \"standby\",\n            ]\n\n        enum_attrs = {event.upper(): event.lower() for event in custom_events}\n        return Enum(f\"{service_name}LifecycleEvents\", enum_attrs)\n\n    @staticmethod\n    def create_processing_events(\n        service_name: str, custom_events: list[str]\n    ) -&gt; type[Enum]:\n        \"\"\"\n        Create a custom ProcessingEvents enum for a service.\n\n        Args:\n            service_name: Name of the service\n            custom_events: List of processing event names\n\n        Returns:\n            Enum class with processing events\n        \"\"\"\n        enum_attrs = {event.upper(): event.lower() for event in custom_events}\n        return Enum(f\"{service_name}ProcessingEvents\", enum_attrs)\n\n    @staticmethod\n    def setup_service_enums(\n        service_name: str, config: dict[str, list[str]]\n    ) -&gt; dict[str, type[Enum]]:\n        \"\"\"\n        Setup all enums for a service at once.\n\n        Args:\n            service_name: Name of the service\n            config: Dictionary with enum types and their events\n\n        Returns:\n            Dictionary mapping enum type names to enum classes\n\n        Example:\n            enums = EnumSetup.setup_service_enums('NLP', {\n                'event_types': ['lifecycle', 'processing', 'analysis'],\n                'lifecycle': ['idle', 'analyzing', 'complete'],\n                'processing': ['tokenize', 'parse', 'classify']\n            })\n        \"\"\"\n        result = {}\n\n        if \"event_types\" in config:\n            result[\"EventTypes\"] = EnumSetup.create_event_types(\n                service_name, config[\"event_types\"]\n            )\n\n        if \"lifecycle\" in config:\n            result[\"LifecycleEvents\"] = EnumSetup.create_lifecycle_events(\n                service_name, config[\"lifecycle\"]\n            )\n\n        if \"processing\" in config:\n            result[\"ProcessingEvents\"] = EnumSetup.create_processing_events(\n                service_name, config[\"processing\"]\n            )\n\n        # Handle any other custom enum types\n        for enum_type, events in config.items():\n            if enum_type not in [\"event_types\", \"lifecycle\", \"processing\"]:\n                enum_name = f\"{enum_type.title()}Events\"\n                enum_attrs = {event.upper(): event.lower() for event in events}\n                result[enum_name] = Enum(f\"{service_name}{enum_name}\", enum_attrs)\n\n        return result\n</code></pre>"},{"location":"api/enums/#champi_signals.enums.EnumSetup.create_event_types","title":"<code>create_event_types(service_name, event_categories=None)</code>  <code>staticmethod</code>","text":"<p>Create a custom EventTypes enum for a service.</p> <p>Parameters:</p> Name Type Description Default <code>service_name</code> <code>str</code> <p>Name of the service (e.g., 'NLP', 'AUDIO')</p> required <code>event_categories</code> <code>list[str]</code> <p>List of event categories. Defaults to standard categories.</p> <code>None</code> <p>Returns:</p> Type Description <code>type[Enum]</code> <p>Enum class with event types</p> Example <p>NLPEventTypes = EnumSetup.create_event_types(     'NLP', ['lifecycle', 'processing', 'analysis'] )</p> Source code in <code>src/champi_signals/enums.py</code> <pre><code>@staticmethod\ndef create_event_types(\n    service_name: str, event_categories: list[str] = None\n) -&gt; type[Enum]:\n    \"\"\"\n    Create a custom EventTypes enum for a service.\n\n    Args:\n        service_name: Name of the service (e.g., 'NLP', 'AUDIO')\n        event_categories: List of event categories. Defaults to standard categories.\n\n    Returns:\n        Enum class with event types\n\n    Example:\n        NLPEventTypes = EnumSetup.create_event_types(\n            'NLP', ['lifecycle', 'processing', 'analysis']\n        )\n    \"\"\"\n    if event_categories is None:\n        event_categories = [\"lifecycle\", \"model\", \"processing\", \"telemetry\"]\n\n    # Create enum attributes\n    enum_attrs = {}\n    for category in event_categories:\n        enum_name = f\"{category.upper()}_EVENT\"\n        enum_value = f\"{category.lower()}_event\"\n        enum_attrs[enum_name] = enum_value\n\n    # Create the enum class dynamically\n    return Enum(f\"{service_name}EventTypes\", enum_attrs)\n</code></pre>"},{"location":"api/enums/#champi_signals.enums.EnumSetup.create_lifecycle_events","title":"<code>create_lifecycle_events(service_name, custom_events=None)</code>  <code>staticmethod</code>","text":"<p>Create a custom LifecycleEvents enum for a service.</p> <p>Parameters:</p> Name Type Description Default <code>service_name</code> <code>str</code> <p>Name of the service</p> required <p>custom_events: Custom lifecycle events. If None, uses standard events.</p> <p>Returns:</p> Type Description <code>type[Enum]</code> <p>Enum class with lifecycle events</p> Source code in <code>src/champi_signals/enums.py</code> <pre><code>@staticmethod\ndef create_lifecycle_events(\n    service_name: str, custom_events: list[str] = None\n) -&gt; type[Enum]:\n    \"\"\"\n    Create a custom LifecycleEvents enum for a service.\n\n    Args:\n        service_name: Name of the service\n    custom_events: Custom lifecycle events. If None, uses standard events.\n\n    Returns:\n        Enum class with lifecycle events\n    \"\"\"\n    if custom_events is None:\n        custom_events = [\n            \"uninitialized\",\n            \"initializing\",\n            \"ready\",\n            \"processing\",\n            \"error\",\n            \"shutdown\",\n            \"stopped\",\n            \"standby\",\n        ]\n\n    enum_attrs = {event.upper(): event.lower() for event in custom_events}\n    return Enum(f\"{service_name}LifecycleEvents\", enum_attrs)\n</code></pre>"},{"location":"api/enums/#champi_signals.enums.EnumSetup.create_processing_events","title":"<code>create_processing_events(service_name, custom_events)</code>  <code>staticmethod</code>","text":"<p>Create a custom ProcessingEvents enum for a service.</p> <p>Parameters:</p> Name Type Description Default <code>service_name</code> <code>str</code> <p>Name of the service</p> required <code>custom_events</code> <code>list[str]</code> <p>List of processing event names</p> required <p>Returns:</p> Type Description <code>type[Enum]</code> <p>Enum class with processing events</p> Source code in <code>src/champi_signals/enums.py</code> <pre><code>@staticmethod\ndef create_processing_events(\n    service_name: str, custom_events: list[str]\n) -&gt; type[Enum]:\n    \"\"\"\n    Create a custom ProcessingEvents enum for a service.\n\n    Args:\n        service_name: Name of the service\n        custom_events: List of processing event names\n\n    Returns:\n        Enum class with processing events\n    \"\"\"\n    enum_attrs = {event.upper(): event.lower() for event in custom_events}\n    return Enum(f\"{service_name}ProcessingEvents\", enum_attrs)\n</code></pre>"},{"location":"api/enums/#champi_signals.enums.EnumSetup.setup_service_enums","title":"<code>setup_service_enums(service_name, config)</code>  <code>staticmethod</code>","text":"<p>Setup all enums for a service at once.</p> <p>Parameters:</p> Name Type Description Default <code>service_name</code> <code>str</code> <p>Name of the service</p> required <code>config</code> <code>dict[str, list[str]]</code> <p>Dictionary with enum types and their events</p> required <p>Returns:</p> Type Description <code>dict[str, type[Enum]]</code> <p>Dictionary mapping enum type names to enum classes</p> Example <p>enums = EnumSetup.setup_service_enums('NLP', {     'event_types': ['lifecycle', 'processing', 'analysis'],     'lifecycle': ['idle', 'analyzing', 'complete'],     'processing': ['tokenize', 'parse', 'classify'] })</p> Source code in <code>src/champi_signals/enums.py</code> <pre><code>@staticmethod\ndef setup_service_enums(\n    service_name: str, config: dict[str, list[str]]\n) -&gt; dict[str, type[Enum]]:\n    \"\"\"\n    Setup all enums for a service at once.\n\n    Args:\n        service_name: Name of the service\n        config: Dictionary with enum types and their events\n\n    Returns:\n        Dictionary mapping enum type names to enum classes\n\n    Example:\n        enums = EnumSetup.setup_service_enums('NLP', {\n            'event_types': ['lifecycle', 'processing', 'analysis'],\n            'lifecycle': ['idle', 'analyzing', 'complete'],\n            'processing': ['tokenize', 'parse', 'classify']\n        })\n    \"\"\"\n    result = {}\n\n    if \"event_types\" in config:\n        result[\"EventTypes\"] = EnumSetup.create_event_types(\n            service_name, config[\"event_types\"]\n        )\n\n    if \"lifecycle\" in config:\n        result[\"LifecycleEvents\"] = EnumSetup.create_lifecycle_events(\n            service_name, config[\"lifecycle\"]\n        )\n\n    if \"processing\" in config:\n        result[\"ProcessingEvents\"] = EnumSetup.create_processing_events(\n            service_name, config[\"processing\"]\n        )\n\n    # Handle any other custom enum types\n    for enum_type, events in config.items():\n        if enum_type not in [\"event_types\", \"lifecycle\", \"processing\"]:\n            enum_name = f\"{enum_type.title()}Events\"\n            enum_attrs = {event.upper(): event.lower() for event in events}\n            result[enum_name] = Enum(f\"{service_name}{enum_name}\", enum_attrs)\n\n    return result\n</code></pre>"},{"location":"api/enums/#champi_signals.enums.ImgUIEventTypes","title":"<code>ImgUIEventTypes</code>","text":"<p>               Bases: <code>IntEnum</code></p> <p>Integer event type codes for ImgUI service.</p> Source code in <code>src/champi_signals/enums.py</code> <pre><code>@unique\nclass ImgUIEventTypes(IntEnum):\n    \"\"\"Integer event type codes for ImgUI service.\"\"\"\n\n    TOOL_CALL_START = 100\n    TOOL_CALL_FINISH = 101\n    TOOL_CALL_ERROR = 102\n    CANVAS_UPDATE = 110\n    WIDGET_CREATE = 120\n    WIDGET_DELETE = 121\n    RENDER_FRAME = 130\n</code></pre>"},{"location":"api/enums/#champi_signals.enums.STTEventTypes","title":"<code>STTEventTypes</code>","text":"<p>               Bases: <code>Enum</code></p> <p>Main event type categories for STT service</p> Source code in <code>src/champi_signals/enums.py</code> <pre><code>@unique\nclass STTEventTypes(Enum):\n    \"\"\"Main event type categories for STT service\"\"\"\n\n    LIFECYCLE_EVENT = \"lifecycle_event\"\n    MODEL_EVENT = \"model_event\"\n    PROCESSING_EVENT = \"processing_event\"\n    TELEMETRY_EVENT = \"telemetry_event\"\n</code></pre>"},{"location":"api/enums/#champi_signals.enums.TTSEventTypes","title":"<code>TTSEventTypes</code>","text":"<p>               Bases: <code>Enum</code></p> <p>Main event type categories for TTS service</p> Source code in <code>src/champi_signals/enums.py</code> <pre><code>@unique\nclass TTSEventTypes(Enum):\n    \"\"\"Main event type categories for TTS service\"\"\"\n\n    LIFECYCLE_EVENT = \"lifecycle_event\"\n    MODEL_EVENT = \"model_event\"\n    PROCESSING_EVENT = \"processing_event\"\n    TELEMETRY_EVENT = \"telemetry_event\"\n</code></pre>"},{"location":"api/enums/#champi_signals.enums.make_event_types","title":"<code>make_event_types(name, spec)</code>","text":"<p>Generate an IntEnum from a declarative spec dict.</p> <p>Each key in spec becomes a prefix group; each value in the list becomes a member named <code>&lt;KEY&gt;_&lt;VALUE&gt;</code>.  Values are assigned sequentially, starting at 1 and incrementing by 1 across all groups in insertion order.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the resulting IntEnum class.</p> required <code>spec</code> <code>dict[str, list[str]]</code> <p>Mapping of group name to list of member suffixes.   Example: <code>{\"PROCESS\": [\"START\", \"FINISH\", \"ERROR\"]}</code></p> required <p>Returns:</p> Type Description <code>type[IntEnum]</code> <p>A new IntEnum subclass with the generated members.</p> <p>Example::</p> <pre><code>MyEvents = make_event_types(\"MyEvents\", {\n    \"PROCESS\": [\"START\", \"FINISH\", \"ERROR\"],\n    \"RENDER\": [\"BEGIN\", \"END\"],\n})\n# MyEvents.PROCESS_START == 1, MyEvents.PROCESS_FINISH == 2, ...\n</code></pre> Source code in <code>src/champi_signals/enums.py</code> <pre><code>def make_event_types(name: str, spec: dict[str, list[str]]) -&gt; type[IntEnum]:\n    \"\"\"\n    Generate an IntEnum from a declarative spec dict.\n\n    Each key in *spec* becomes a prefix group; each value in the list becomes\n    a member named ``&lt;KEY&gt;_&lt;VALUE&gt;``.  Values are assigned sequentially,\n    starting at 1 and incrementing by 1 across all groups in insertion order.\n\n    Args:\n        name: Name of the resulting IntEnum class.\n        spec: Mapping of group name to list of member suffixes.\n              Example: ``{\"PROCESS\": [\"START\", \"FINISH\", \"ERROR\"]}``\n\n    Returns:\n        A new IntEnum subclass with the generated members.\n\n    Example::\n\n        MyEvents = make_event_types(\"MyEvents\", {\n            \"PROCESS\": [\"START\", \"FINISH\", \"ERROR\"],\n            \"RENDER\": [\"BEGIN\", \"END\"],\n        })\n        # MyEvents.PROCESS_START == 1, MyEvents.PROCESS_FINISH == 2, ...\n    \"\"\"\n    members: dict[str, int] = {}\n    counter = 1\n    for group, suffixes in spec.items():\n        for suffix in suffixes:\n            members[f\"{group}_{suffix}\"] = counter\n            counter += 1\n    return IntEnum(name, members)  # type: ignore[return-value]\n</code></pre>"},{"location":"api/managers/","title":"managers","text":"<p>Signal manager abstract base class and default implementation.</p> <p>Signal managers extracted from Champi services.</p>"},{"location":"api/managers/#champi_signals.managers.BaseSignalManager","title":"<code>BaseSignalManager</code>","text":"<p>               Bases: <code>SignalManagerABC</code></p> <p>Base signal manager with common functionality.</p> Source code in <code>src/champi_signals/managers.py</code> <pre><code>class BaseSignalManager(SignalManagerABC):\n    \"\"\"Base signal manager with common functionality.\"\"\"\n\n    _instance = None\n    _signals_initialized = False\n\n    def __new__(cls):\n        \"\"\"Singleton pattern implementation\"\"\"\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n\n    def __init__(self):\n        \"\"\"Initialize singleton instance only once\"\"\"\n        if not self._signals_initialized:\n            self.signals: dict[str, Signal] = {}\n            self._class_signals: dict[str, Signal] = {}\n            type(self)._signals_initialized = True\n\n    def get_signals(self) -&gt; list[Signal]:\n        \"\"\"Get all registered signals.\"\"\"\n        return list(self.signals.values())\n\n    def setup_custom_signals(self, signal_config: dict[str, type[Enum]]) -&gt; None:\n        \"\"\"Setup custom signals based on provided enum configuration.\n\n        Args:\n            signal_config: Dictionary mapping signal names to their enum types\n        \"\"\"\n        for signal_name, enum_type in signal_config.items():\n            # Get the first enum value as the signal identifier\n            signal_identifier = list(enum_type)[0].value if enum_type else signal_name\n            signal_obj = Signal(signal_identifier)\n\n            # Store the signal\n            self.signals[signal_name] = signal_obj\n            self._class_signals[f\"_{signal_name}_signal\"] = signal_obj\n\n            # Create property accessor dynamically\n            self._create_signal_property(signal_name, signal_obj)\n\n    def _create_signal_property(self, signal_name: str, signal_obj: Signal) -&gt; None:\n        \"\"\"Create getter/setter properties for a signal.\"\"\"\n\n        def getter(_self):\n            return signal_obj\n\n        def setter(_self, receiver: Callable):\n            signal_obj.connect(receiver)\n\n        # Set the property on the class\n        setattr(type(self), signal_name, property(getter, setter))\n</code></pre>"},{"location":"api/managers/#champi_signals.managers.BaseSignalManager.__init__","title":"<code>__init__()</code>","text":"<p>Initialize singleton instance only once</p> Source code in <code>src/champi_signals/managers.py</code> <pre><code>def __init__(self):\n    \"\"\"Initialize singleton instance only once\"\"\"\n    if not self._signals_initialized:\n        self.signals: dict[str, Signal] = {}\n        self._class_signals: dict[str, Signal] = {}\n        type(self)._signals_initialized = True\n</code></pre>"},{"location":"api/managers/#champi_signals.managers.BaseSignalManager.__new__","title":"<code>__new__()</code>","text":"<p>Singleton pattern implementation</p> Source code in <code>src/champi_signals/managers.py</code> <pre><code>def __new__(cls):\n    \"\"\"Singleton pattern implementation\"\"\"\n    if cls._instance is None:\n        cls._instance = super().__new__(cls)\n    return cls._instance\n</code></pre>"},{"location":"api/managers/#champi_signals.managers.BaseSignalManager.get_signals","title":"<code>get_signals()</code>","text":"<p>Get all registered signals.</p> Source code in <code>src/champi_signals/managers.py</code> <pre><code>def get_signals(self) -&gt; list[Signal]:\n    \"\"\"Get all registered signals.\"\"\"\n    return list(self.signals.values())\n</code></pre>"},{"location":"api/managers/#champi_signals.managers.BaseSignalManager.setup_custom_signals","title":"<code>setup_custom_signals(signal_config)</code>","text":"<p>Setup custom signals based on provided enum configuration.</p> <p>Parameters:</p> Name Type Description Default <code>signal_config</code> <code>dict[str, type[Enum]]</code> <p>Dictionary mapping signal names to their enum types</p> required Source code in <code>src/champi_signals/managers.py</code> <pre><code>def setup_custom_signals(self, signal_config: dict[str, type[Enum]]) -&gt; None:\n    \"\"\"Setup custom signals based on provided enum configuration.\n\n    Args:\n        signal_config: Dictionary mapping signal names to their enum types\n    \"\"\"\n    for signal_name, enum_type in signal_config.items():\n        # Get the first enum value as the signal identifier\n        signal_identifier = list(enum_type)[0].value if enum_type else signal_name\n        signal_obj = Signal(signal_identifier)\n\n        # Store the signal\n        self.signals[signal_name] = signal_obj\n        self._class_signals[f\"_{signal_name}_signal\"] = signal_obj\n\n        # Create property accessor dynamically\n        self._create_signal_property(signal_name, signal_obj)\n</code></pre>"},{"location":"api/managers/#champi_signals.managers.SignalManagerABC","title":"<code>SignalManagerABC</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for signal managers.</p> Source code in <code>src/champi_signals/managers.py</code> <pre><code>class SignalManagerABC(ABC):\n    \"\"\"Abstract base class for signal managers.\"\"\"\n\n    @abstractmethod\n    def setup_custom_signals(self, signal_config: dict[str, type[Enum]]) -&gt; None:\n        \"\"\"Setup custom signals based on provided enum configuration.\n\n        Args:\n            signal_config: Dictionary mapping signal names to their enum types\n                          e.g., {'lifecycle': MyLifecycleEnum, 'processing': MyProcessingEnum}\n        \"\"\"\n        pass\n</code></pre>"},{"location":"api/managers/#champi_signals.managers.SignalManagerABC.setup_custom_signals","title":"<code>setup_custom_signals(signal_config)</code>  <code>abstractmethod</code>","text":"<p>Setup custom signals based on provided enum configuration.</p> <p>Parameters:</p> Name Type Description Default <code>signal_config</code> <code>dict[str, type[Enum]]</code> <p>Dictionary mapping signal names to their enum types           e.g., {'lifecycle': MyLifecycleEnum, 'processing': MyProcessingEnum}</p> required Source code in <code>src/champi_signals/managers.py</code> <pre><code>@abstractmethod\ndef setup_custom_signals(self, signal_config: dict[str, type[Enum]]) -&gt; None:\n    \"\"\"Setup custom signals based on provided enum configuration.\n\n    Args:\n        signal_config: Dictionary mapping signal names to their enum types\n                      e.g., {'lifecycle': MyLifecycleEnum, 'processing': MyProcessingEnum}\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/processors/","title":"processors","text":"<p>Event processor decorator for typed event handler registration.</p> <p>Event processor extracted from Champi services.</p>"},{"location":"api/processors/#champi_signals.processors.EventProcessor","title":"<code>EventProcessor</code>","text":"<p>Event processing decorators for automatic event emission</p> Source code in <code>src/champi_signals/processors.py</code> <pre><code>class EventProcessor:\n    \"\"\"Event processing decorators for automatic event emission\"\"\"\n\n    @staticmethod\n    def emits_event(data=None, **metadata):\n        \"\"\"\n        Static decorator for START/FINISH/ERROR event emission with state capture\n\n        Args:\n            data: List of variable names to track (supports 'cls.var' for class vars)\n            **metadata: Additional metadata to include in all events\n        \"\"\"\n\n        def decorator(func):\n            if asyncio.iscoroutinefunction(func):\n\n                async def async_wrapper(self, *args, **kwargs):\n                    return await _execute_with_events_async(\n                        func, self, args, kwargs, data, metadata\n                    )\n\n                return async_wrapper\n            else:\n\n                def sync_wrapper(self, *args, **kwargs):\n                    return _execute_with_events(\n                        func, self, args, kwargs, data, metadata, False\n                    )\n\n                return sync_wrapper\n\n        return decorator\n\n    @staticmethod\n    def periodic_emit(variables=None, interval=3.0):\n        \"\"\"\n        Static decorator for continuous variable monitoring during method execution\n\n        Args:\n            variables: List of variable names to monitor (supports 'cls.var' for class vars)\n            interval: Monitoring interval in seconds\n        \"\"\"\n\n        def decorator(func):\n            if not asyncio.iscoroutinefunction(func):\n                # Periodic monitoring only works with async functions\n                return func\n\n            async def wrapper(self, *args, **kwargs):\n                # Safely extract event configuration\n                try:\n                    event_type = getattr(self.__class__, \"Meta\", None)\n                    if event_type and hasattr(event_type, \"event_type\"):\n                        event_type = event_type.event_type\n                    else:\n                        event_type = \"processing\"\n                except Exception:\n                    event_type = \"processing\"\n\n                # Extract signal manager from service metaclass\n                signals = None\n                try:\n                    meta = getattr(self.__class__, \"Meta\", None)\n                    if meta and hasattr(meta, \"signal_manager\"):\n                        signals = meta.signal_manager\n                    elif meta and hasattr(meta, \"signal_manager_class\"):\n                        signals = meta.signal_manager_class()\n                except Exception:\n                    pass\n\n                if signals is None:\n                    # No signal manager found, skip monitoring\n                    return await func(self, *args, **kwargs)\n\n                # Get target signal\n                try:\n                    signal = getattr(signals, event_type)\n                except AttributeError:\n                    signal = (\n                        signals.signals.get(\"processing\")\n                        if hasattr(signals, \"signals\")\n                        else None\n                    )\n\n                if signal is None:\n                    # No valid signal found, skip monitoring\n                    return await func(self, *args, **kwargs)\n\n                method_name = func.__name__.upper()\n                monitoring = True\n                monitor_task = None\n\n                async def monitor_vars():\n                    \"\"\"Background task for variable monitoring\"\"\"\n                    last_values = {}\n\n                    while monitoring:\n                        try:\n                            current_values = {\n                                \"timestamp\": asyncio.get_event_loop().time()\n                            }\n\n                            # Capture current variable state\n                            for var_name in variables or []:\n                                try:\n                                    if var_name.startswith(\"cls.\"):\n                                        attr_name = var_name[4:]\n                                        if hasattr(self.__class__, attr_name):\n                                            current_values[attr_name] = getattr(\n                                                self.__class__, attr_name, None\n                                            )\n                                    else:\n                                        if hasattr(self, var_name):\n                                            current_values[var_name] = getattr(\n                                                self, var_name, None\n                                            )\n                                except Exception as e:\n                                    current_values[f\"{var_name}_error\"] = (\n                                        f\"Monitor error: {str(e)}\"\n                                    )\n\n                            # Emit if values changed (excluding timestamp)\n                            values_without_timestamp = {\n                                k: v\n                                for k, v in current_values.items()\n                                if k != \"timestamp\"\n                            }\n                            last_without_timestamp = {\n                                k: v for k, v in last_values.items() if k != \"timestamp\"\n                            }\n\n                            if values_without_timestamp != last_without_timestamp:\n                                try:\n                                    signal.send(\n                                        event_type=event_type,\n                                        sub_event=f\"{method_name}_PROGRESS\",\n                                        data=current_values,\n                                    )\n                                except Exception:\n                                    pass\n\n                                last_values = current_values.copy()\n\n                            await asyncio.sleep(interval)\n\n                        except asyncio.CancelledError:\n                            break\n                        except Exception:\n                            # Continue monitoring even if individual cycles fail\n                            await asyncio.sleep(interval)\n\n                # Start background monitoring\n                try:\n                    monitor_task = asyncio.create_task(monitor_vars())\n                except Exception:\n                    monitor_task = None\n\n                try:\n                    # Execute original method\n                    result = await func(self, *args, **kwargs)\n                    return result\n\n                finally:\n                    # Clean up monitoring\n                    monitoring = False\n                    if monitor_task and not monitor_task.done():\n                        monitor_task.cancel()\n                        try:\n                            await monitor_task\n                        except (asyncio.CancelledError, Exception):\n                            pass\n\n            return wrapper\n\n        return decorator\n\n    @classmethod\n    def emits_all_events(cls, data=None):\n        \"\"\"\n        Class decorator that applies ``emits_event`` to every public, non-dunder\n        method on the decorated class, excluding any inner ``Meta`` class.\n\n        Args:\n            data: List of variable names to track, forwarded to each ``emits_event``\n                call (supports ``'cls.var'`` for class-level attributes).\n\n        Returns:\n            A class decorator that wraps all eligible methods with event emission.\n\n        Example::\n\n            @EventProcessor.emits_all_events(data=[\"status\"])\n            class MyService:\n                class Meta:\n                    event_type = \"processing\"\n                    signal_manager = my_signals\n        \"\"\"\n\n        def decorator(klass):\n            for name, method in inspect.getmembers(klass, predicate=inspect.isfunction):\n                if name.startswith(\"_\"):\n                    continue\n                if name == \"Meta\":\n                    continue\n                wrapped = cls.emits_event(data=data)(method)\n                setattr(klass, name, wrapped)\n            return klass\n\n        return decorator\n\n    @staticmethod\n    @asynccontextmanager\n    async def context(signal_manager, event_type, data=None):\n        \"\"\"\n        Async context manager that emits lifecycle events around a code block.\n\n        Emits ``&lt;event_type&gt;_START`` on entry, ``&lt;event_type&gt;_FINISH`` on clean\n        exit, and ``&lt;event_type&gt;_ERROR`` (then re-raises) if an exception\n        propagates.\n\n        Args:\n            signal_manager: A signal manager instance whose attribute named\n                ``event_type`` is a signal accepting ``send(event_type=...,\n                sub_event=..., data=...)``.\n            event_type: String key used to look up the signal on\n                ``signal_manager`` and as the ``event_type`` value in emitted\n                events.\n            data: Optional dict of arbitrary data included in every emitted event.\n\n        Raises:\n            Exception: Any exception raised inside the ``async with`` block is\n                re-raised after the ``&lt;event_type&gt;_ERROR`` event is emitted.\n\n        Example::\n\n            async with EventProcessor.context(signals, \"processing\", data={\"job\": 1}):\n                await do_work()\n        \"\"\"\n        try:\n            signal = getattr(signal_manager, event_type)\n        except AttributeError:\n            signal = None\n\n        event_data = dict(data or {})\n\n        if signal is not None:\n            try:\n                signal.send(\n                    event_type=event_type,\n                    sub_event=f\"{event_type.upper()}_START\",\n                    data=event_data,\n                )\n            except Exception:\n                pass\n\n        try:\n            yield\n        except Exception as ex:\n            if signal is not None:\n                try:\n                    signal.send(\n                        event_type=event_type,\n                        sub_event=f\"{event_type.upper()}_ERROR\",\n                        data={\n                            **event_data,\n                            \"error\": str(ex),\n                            \"error_type\": type(ex).__name__,\n                        },\n                    )\n                except Exception:\n                    pass\n            raise\n        else:\n            if signal is not None:\n                try:\n                    signal.send(\n                        event_type=event_type,\n                        sub_event=f\"{event_type.upper()}_FINISH\",\n                        data=event_data,\n                    )\n                except Exception:\n                    pass\n</code></pre>"},{"location":"api/processors/#champi_signals.processors.EventProcessor.context","title":"<code>context(signal_manager, event_type, data=None)</code>  <code>async</code> <code>staticmethod</code>","text":"<p>Async context manager that emits lifecycle events around a code block.</p> <p>Emits <code>&lt;event_type&gt;_START</code> on entry, <code>&lt;event_type&gt;_FINISH</code> on clean exit, and <code>&lt;event_type&gt;_ERROR</code> (then re-raises) if an exception propagates.</p> <p>Parameters:</p> Name Type Description Default <code>signal_manager</code> <p>A signal manager instance whose attribute named <code>event_type</code> is a signal accepting <code>send(event_type=..., sub_event=..., data=...)</code>.</p> required <code>event_type</code> <p>String key used to look up the signal on <code>signal_manager</code> and as the <code>event_type</code> value in emitted events.</p> required <code>data</code> <p>Optional dict of arbitrary data included in every emitted event.</p> <code>None</code> <p>Raises:</p> Type Description <code>Exception</code> <p>Any exception raised inside the <code>async with</code> block is re-raised after the <code>&lt;event_type&gt;_ERROR</code> event is emitted.</p> <p>Example::</p> <pre><code>async with EventProcessor.context(signals, \"processing\", data={\"job\": 1}):\n    await do_work()\n</code></pre> Source code in <code>src/champi_signals/processors.py</code> <pre><code>@staticmethod\n@asynccontextmanager\nasync def context(signal_manager, event_type, data=None):\n    \"\"\"\n    Async context manager that emits lifecycle events around a code block.\n\n    Emits ``&lt;event_type&gt;_START`` on entry, ``&lt;event_type&gt;_FINISH`` on clean\n    exit, and ``&lt;event_type&gt;_ERROR`` (then re-raises) if an exception\n    propagates.\n\n    Args:\n        signal_manager: A signal manager instance whose attribute named\n            ``event_type`` is a signal accepting ``send(event_type=...,\n            sub_event=..., data=...)``.\n        event_type: String key used to look up the signal on\n            ``signal_manager`` and as the ``event_type`` value in emitted\n            events.\n        data: Optional dict of arbitrary data included in every emitted event.\n\n    Raises:\n        Exception: Any exception raised inside the ``async with`` block is\n            re-raised after the ``&lt;event_type&gt;_ERROR`` event is emitted.\n\n    Example::\n\n        async with EventProcessor.context(signals, \"processing\", data={\"job\": 1}):\n            await do_work()\n    \"\"\"\n    try:\n        signal = getattr(signal_manager, event_type)\n    except AttributeError:\n        signal = None\n\n    event_data = dict(data or {})\n\n    if signal is not None:\n        try:\n            signal.send(\n                event_type=event_type,\n                sub_event=f\"{event_type.upper()}_START\",\n                data=event_data,\n            )\n        except Exception:\n            pass\n\n    try:\n        yield\n    except Exception as ex:\n        if signal is not None:\n            try:\n                signal.send(\n                    event_type=event_type,\n                    sub_event=f\"{event_type.upper()}_ERROR\",\n                    data={\n                        **event_data,\n                        \"error\": str(ex),\n                        \"error_type\": type(ex).__name__,\n                    },\n                )\n            except Exception:\n                pass\n        raise\n    else:\n        if signal is not None:\n            try:\n                signal.send(\n                    event_type=event_type,\n                    sub_event=f\"{event_type.upper()}_FINISH\",\n                    data=event_data,\n                )\n            except Exception:\n                pass\n</code></pre>"},{"location":"api/processors/#champi_signals.processors.EventProcessor.emits_all_events","title":"<code>emits_all_events(data=None)</code>  <code>classmethod</code>","text":"<p>Class decorator that applies <code>emits_event</code> to every public, non-dunder method on the decorated class, excluding any inner <code>Meta</code> class.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <p>List of variable names to track, forwarded to each <code>emits_event</code> call (supports <code>'cls.var'</code> for class-level attributes).</p> <code>None</code> <p>Returns:</p> Type Description <p>A class decorator that wraps all eligible methods with event emission.</p> <p>Example::</p> <pre><code>@EventProcessor.emits_all_events(data=[\"status\"])\nclass MyService:\n    class Meta:\n        event_type = \"processing\"\n        signal_manager = my_signals\n</code></pre> Source code in <code>src/champi_signals/processors.py</code> <pre><code>@classmethod\ndef emits_all_events(cls, data=None):\n    \"\"\"\n    Class decorator that applies ``emits_event`` to every public, non-dunder\n    method on the decorated class, excluding any inner ``Meta`` class.\n\n    Args:\n        data: List of variable names to track, forwarded to each ``emits_event``\n            call (supports ``'cls.var'`` for class-level attributes).\n\n    Returns:\n        A class decorator that wraps all eligible methods with event emission.\n\n    Example::\n\n        @EventProcessor.emits_all_events(data=[\"status\"])\n        class MyService:\n            class Meta:\n                event_type = \"processing\"\n                signal_manager = my_signals\n    \"\"\"\n\n    def decorator(klass):\n        for name, method in inspect.getmembers(klass, predicate=inspect.isfunction):\n            if name.startswith(\"_\"):\n                continue\n            if name == \"Meta\":\n                continue\n            wrapped = cls.emits_event(data=data)(method)\n            setattr(klass, name, wrapped)\n        return klass\n\n    return decorator\n</code></pre>"},{"location":"api/processors/#champi_signals.processors.EventProcessor.emits_event","title":"<code>emits_event(data=None, **metadata)</code>  <code>staticmethod</code>","text":"<p>Static decorator for START/FINISH/ERROR event emission with state capture</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <p>List of variable names to track (supports 'cls.var' for class vars)</p> <code>None</code> <code>**metadata</code> <p>Additional metadata to include in all events</p> <code>{}</code> Source code in <code>src/champi_signals/processors.py</code> <pre><code>@staticmethod\ndef emits_event(data=None, **metadata):\n    \"\"\"\n    Static decorator for START/FINISH/ERROR event emission with state capture\n\n    Args:\n        data: List of variable names to track (supports 'cls.var' for class vars)\n        **metadata: Additional metadata to include in all events\n    \"\"\"\n\n    def decorator(func):\n        if asyncio.iscoroutinefunction(func):\n\n            async def async_wrapper(self, *args, **kwargs):\n                return await _execute_with_events_async(\n                    func, self, args, kwargs, data, metadata\n                )\n\n            return async_wrapper\n        else:\n\n            def sync_wrapper(self, *args, **kwargs):\n                return _execute_with_events(\n                    func, self, args, kwargs, data, metadata, False\n                )\n\n            return sync_wrapper\n\n    return decorator\n</code></pre>"},{"location":"api/processors/#champi_signals.processors.EventProcessor.periodic_emit","title":"<code>periodic_emit(variables=None, interval=3.0)</code>  <code>staticmethod</code>","text":"<p>Static decorator for continuous variable monitoring during method execution</p> <p>Parameters:</p> Name Type Description Default <code>variables</code> <p>List of variable names to monitor (supports 'cls.var' for class vars)</p> <code>None</code> <code>interval</code> <p>Monitoring interval in seconds</p> <code>3.0</code> Source code in <code>src/champi_signals/processors.py</code> <pre><code>@staticmethod\ndef periodic_emit(variables=None, interval=3.0):\n    \"\"\"\n    Static decorator for continuous variable monitoring during method execution\n\n    Args:\n        variables: List of variable names to monitor (supports 'cls.var' for class vars)\n        interval: Monitoring interval in seconds\n    \"\"\"\n\n    def decorator(func):\n        if not asyncio.iscoroutinefunction(func):\n            # Periodic monitoring only works with async functions\n            return func\n\n        async def wrapper(self, *args, **kwargs):\n            # Safely extract event configuration\n            try:\n                event_type = getattr(self.__class__, \"Meta\", None)\n                if event_type and hasattr(event_type, \"event_type\"):\n                    event_type = event_type.event_type\n                else:\n                    event_type = \"processing\"\n            except Exception:\n                event_type = \"processing\"\n\n            # Extract signal manager from service metaclass\n            signals = None\n            try:\n                meta = getattr(self.__class__, \"Meta\", None)\n                if meta and hasattr(meta, \"signal_manager\"):\n                    signals = meta.signal_manager\n                elif meta and hasattr(meta, \"signal_manager_class\"):\n                    signals = meta.signal_manager_class()\n            except Exception:\n                pass\n\n            if signals is None:\n                # No signal manager found, skip monitoring\n                return await func(self, *args, **kwargs)\n\n            # Get target signal\n            try:\n                signal = getattr(signals, event_type)\n            except AttributeError:\n                signal = (\n                    signals.signals.get(\"processing\")\n                    if hasattr(signals, \"signals\")\n                    else None\n                )\n\n            if signal is None:\n                # No valid signal found, skip monitoring\n                return await func(self, *args, **kwargs)\n\n            method_name = func.__name__.upper()\n            monitoring = True\n            monitor_task = None\n\n            async def monitor_vars():\n                \"\"\"Background task for variable monitoring\"\"\"\n                last_values = {}\n\n                while monitoring:\n                    try:\n                        current_values = {\n                            \"timestamp\": asyncio.get_event_loop().time()\n                        }\n\n                        # Capture current variable state\n                        for var_name in variables or []:\n                            try:\n                                if var_name.startswith(\"cls.\"):\n                                    attr_name = var_name[4:]\n                                    if hasattr(self.__class__, attr_name):\n                                        current_values[attr_name] = getattr(\n                                            self.__class__, attr_name, None\n                                        )\n                                else:\n                                    if hasattr(self, var_name):\n                                        current_values[var_name] = getattr(\n                                            self, var_name, None\n                                        )\n                            except Exception as e:\n                                current_values[f\"{var_name}_error\"] = (\n                                    f\"Monitor error: {str(e)}\"\n                                )\n\n                        # Emit if values changed (excluding timestamp)\n                        values_without_timestamp = {\n                            k: v\n                            for k, v in current_values.items()\n                            if k != \"timestamp\"\n                        }\n                        last_without_timestamp = {\n                            k: v for k, v in last_values.items() if k != \"timestamp\"\n                        }\n\n                        if values_without_timestamp != last_without_timestamp:\n                            try:\n                                signal.send(\n                                    event_type=event_type,\n                                    sub_event=f\"{method_name}_PROGRESS\",\n                                    data=current_values,\n                                )\n                            except Exception:\n                                pass\n\n                            last_values = current_values.copy()\n\n                        await asyncio.sleep(interval)\n\n                    except asyncio.CancelledError:\n                        break\n                    except Exception:\n                        # Continue monitoring even if individual cycles fail\n                        await asyncio.sleep(interval)\n\n            # Start background monitoring\n            try:\n                monitor_task = asyncio.create_task(monitor_vars())\n            except Exception:\n                monitor_task = None\n\n            try:\n                # Execute original method\n                result = await func(self, *args, **kwargs)\n                return result\n\n            finally:\n                # Clean up monitoring\n                monitoring = False\n                if monitor_task and not monitor_task.done():\n                    monitor_task.cancel()\n                    try:\n                        await monitor_task\n                    except (asyncio.CancelledError, Exception):\n                        pass\n\n        return wrapper\n\n    return decorator\n</code></pre>"},{"location":"phases/phase-0-foundation/","title":"Phase 0: Foundation","text":""},{"location":"phases/phase-0-foundation/#goal","title":"Goal","text":"<p>CI pipeline enforces quality gates and the repo is ready for feature development on a dedicated branch.</p>"},{"location":"phases/phase-0-foundation/#deliverables","title":"Deliverables","text":""},{"location":"phases/phase-0-foundation/#backend","title":"Backend","text":"<ul> <li>[ ] Create <code>src/champi_signals/bridges.py</code> module stub (empty file with docstring)</li> <li>[ ] Verify existing tests pass on Python 3.12</li> </ul>"},{"location":"phases/phase-0-foundation/#frontend","title":"Frontend","text":"<ul> <li>[ ] Confirm all existing public exports are tested and documented in <code>__init__.py</code></li> </ul>"},{"location":"phases/phase-0-foundation/#infrastructure","title":"Infrastructure","text":"<ul> <li>[ ] Add <code>release.yml</code> workflow (trigger on <code>v*</code> tag, <code>uv build</code> + <code>uv publish</code> via trusted publisher)</li> <li>[ ] Add <code>docs.yml</code> workflow (build mkdocs on push to main, deploy to GitHub Pages)</li> <li>[ ] Add docs dev-dependency group to <code>pyproject.toml</code></li> <li>[ ] Configure coverage enforcement (<code>--cov-fail-under=90</code>) in CI</li> </ul>"},{"location":"phases/phase-0-foundation/#done-definition","title":"Done Definition","text":"<ul> <li><code>ci.yml</code> runs lint + test + coverage gate on every PR</li> <li><code>release.yml</code> exists and is syntactically valid (dry-run with <code>act</code> or manual inspection)</li> <li><code>docs.yml</code> exists and builds mkdocs locally without errors</li> <li>All existing tests pass with &gt;= 90% coverage</li> </ul>"},{"location":"phases/phase-0-foundation/#parallel-work","title":"Parallel work","text":"<ul> <li>BE: stub bridges.py can run alongside Infra: CI workflow additions</li> </ul>"},{"location":"phases/phase-0-foundation/#phase-dependencies","title":"Phase dependencies","text":"<ul> <li>Requires: none</li> </ul>"},{"location":"phases/phase-0-foundation/#complexity","title":"Complexity","text":"<ul> <li>Backend: S</li> <li>Frontend: S</li> <li>Infra: M</li> </ul>"},{"location":"phases/phase-0-foundation/#risks","title":"Risks","text":"<ul> <li>PyPI trusted publisher OIDC setup requires manual configuration on pypi.org (cannot be automated in CI alone)</li> </ul>"},{"location":"phases/phase-1-ipc-bridge/","title":"Phase 1: IPC Bridge Interface","text":""},{"location":"phases/phase-1-ipc-bridge/#goal","title":"Goal","text":"<p><code>SignalBridgeABC</code> is implemented, tested, and exported so champi-ipc can depend on it.</p>"},{"location":"phases/phase-1-ipc-bridge/#deliverables","title":"Deliverables","text":""},{"location":"phases/phase-1-ipc-bridge/#backend","title":"Backend","text":"<ul> <li>[ ] Implement <code>SignalBridgeABC</code> in <code>bridges.py</code> with <code>connect</code>, <code>disconnect</code>, <code>start</code>, <code>stop</code>, and <code>is_connected</code> property</li> <li>[ ] Write <code>tests/test_bridges.py</code> with concrete stub that records calls</li> <li>[ ] Test async <code>start</code>/<code>stop</code> lifecycle</li> <li>[ ] Test <code>is_connected</code> property behavior</li> </ul>"},{"location":"phases/phase-1-ipc-bridge/#frontend","title":"Frontend","text":"<ul> <li>[ ] Export <code>SignalBridgeABC</code> from <code>__init__.py</code></li> <li>[ ] Add type stub coverage in <code>py.typed</code></li> </ul>"},{"location":"phases/phase-1-ipc-bridge/#infrastructure","title":"Infrastructure","text":"<ul> <li>[ ] None</li> </ul>"},{"location":"phases/phase-1-ipc-bridge/#done-definition","title":"Done Definition","text":"<ul> <li><code>from champi_signals import SignalBridgeABC</code> works</li> <li>A concrete subclass can be instantiated, connected to a <code>BaseSignalManager</code>, started, and stopped</li> <li><code>is_connected</code> returns correct state before/after connect/disconnect</li> <li>100% line coverage on <code>bridges.py</code></li> </ul>"},{"location":"phases/phase-1-ipc-bridge/#parallel-work","title":"Parallel work","text":"<ul> <li>BE: bridges implementation can run alongside FE: init.py export updates (merge at end)</li> </ul>"},{"location":"phases/phase-1-ipc-bridge/#phase-dependencies","title":"Phase dependencies","text":"<ul> <li>Requires: Phase 0 (CI pipeline with coverage gate)</li> </ul>"},{"location":"phases/phase-1-ipc-bridge/#complexity","title":"Complexity","text":"<ul> <li>Backend: S</li> <li>Frontend: S</li> <li>Infra: S</li> </ul>"},{"location":"phases/phase-1-ipc-bridge/#risks","title":"Risks","text":"<ul> <li>ABC design may need revision once champi-ipc starts implementing it; keep interface minimal</li> </ul>"},{"location":"phases/phase-2-enhanced-processor/","title":"Phase 2: Enhanced EventProcessor","text":""},{"location":"phases/phase-2-enhanced-processor/#goal","title":"Goal","text":"<p><code>EventProcessor</code> supports class-level decoration and async context manager for imperative event emission.</p>"},{"location":"phases/phase-2-enhanced-processor/#deliverables","title":"Deliverables","text":""},{"location":"phases/phase-2-enhanced-processor/#backend","title":"Backend","text":"<ul> <li>[ ] Implement <code>EventProcessor.emits_all_events(data=[...])</code> class decorator in <code>processors.py</code></li> <li>[ ] Implement <code>EventProcessor.context(signal_manager, event_type, data)</code> async context manager</li> <li>[ ] Add tests for class-level decoration (happy path, skips dunders/private, respects Meta)</li> <li>[ ] Add tests for context manager (happy path, error path with re-raise)</li> </ul>"},{"location":"phases/phase-2-enhanced-processor/#frontend","title":"Frontend","text":"<ul> <li>[ ] No new exports needed (methods added to existing <code>EventProcessor</code> class)</li> <li>[ ] Update docstrings for new methods</li> </ul>"},{"location":"phases/phase-2-enhanced-processor/#infrastructure","title":"Infrastructure","text":"<ul> <li>[ ] None</li> </ul>"},{"location":"phases/phase-2-enhanced-processor/#done-definition","title":"Done Definition","text":"<ul> <li><code>@EventProcessor.emits_all_events(data=['status'])</code> decorates all public methods of a class</li> <li>Private and dunder methods are skipped</li> <li><code>async with EventProcessor.context(mgr, \"reading\", data={...})</code> emits START/FINISH or START/ERROR</li> <li>Error path re-raises the original exception</li> <li>Existing <code>@EventProcessor.emits_event</code> tests still pass (no regressions)</li> </ul>"},{"location":"phases/phase-2-enhanced-processor/#parallel-work","title":"Parallel work","text":"<ul> <li>BE: class decorator and context manager can be developed in parallel by two developers</li> </ul>"},{"location":"phases/phase-2-enhanced-processor/#phase-dependencies","title":"Phase dependencies","text":"<ul> <li>Requires: Phase 0 (CI pipeline)</li> </ul>"},{"location":"phases/phase-2-enhanced-processor/#complexity","title":"Complexity","text":"<ul> <li>Backend: M</li> <li>Frontend: S</li> <li>Infra: S</li> </ul>"},{"location":"phases/phase-2-enhanced-processor/#risks","title":"Risks","text":"<ul> <li><code>inspect.getmembers</code> behavior with async methods, properties, and classmethods needs careful testing</li> <li>Class decorator interaction with <code>__init_subclass__</code> in downstream inheritance chains</li> </ul>"},{"location":"phases/phase-3-enums-and-docs/","title":"Phase 3: Enum Expansion and Documentation","text":""},{"location":"phases/phase-3-enums-and-docs/#goal","title":"Goal","text":"<p><code>ImgUIEventTypes</code> and <code>make_event_types()</code> are available, mkdocs site is live, and v0.2.0 is ready to release.</p>"},{"location":"phases/phase-3-enums-and-docs/#deliverables","title":"Deliverables","text":""},{"location":"phases/phase-3-enums-and-docs/#backend","title":"Backend","text":"<ul> <li>[ ] Add <code>ImgUIEventTypes</code> IntEnum to <code>enums.py</code> (values starting at 100)</li> <li>[ ] Implement <code>make_event_types(name, spec)</code> factory function in <code>enums.py</code></li> <li>[ ] Add tests for <code>ImgUIEventTypes</code> values and membership</li> <li>[ ] Add tests for <code>make_event_types</code> (generates correct enum members and values)</li> </ul>"},{"location":"phases/phase-3-enums-and-docs/#frontend","title":"Frontend","text":"<ul> <li>[ ] Export <code>ImgUIEventTypes</code> and <code>make_event_types</code> from <code>__init__.py</code></li> <li>[ ] Write mkdocs content: Getting Started, API Reference, Integration Guide, Changelog</li> <li>[ ] Add <code>mkdocs.yml</code> configuration</li> </ul>"},{"location":"phases/phase-3-enums-and-docs/#infrastructure","title":"Infrastructure","text":"<ul> <li>[ ] Verify <code>docs.yml</code> deploys mkdocs site to GitHub Pages</li> <li>[ ] Verify <code>release.yml</code> publishes to PyPI on <code>v0.2.0</code> tag</li> <li>[ ] Run <code>cz bump</code> to create v0.2.0 tag and changelog</li> </ul>"},{"location":"phases/phase-3-enums-and-docs/#done-definition","title":"Done Definition","text":"<ul> <li><code>from champi_signals import ImgUIEventTypes, make_event_types</code> works</li> <li><code>ImgUIEventTypes.TOOL_CALL_START == 100</code></li> <li><code>make_event_types(\"X\", {\"A\": [\"B\"]})</code> returns an IntEnum with member <code>A_B</code></li> <li>mkdocs site builds without warnings and includes all public API symbols</li> <li>v0.2.0 tag triggers successful PyPI publish (or dry-run passes)</li> <li>Overall test coverage &gt;= 90%</li> </ul>"},{"location":"phases/phase-3-enums-and-docs/#parallel-work","title":"Parallel work","text":"<ul> <li>BE: enum implementation can run alongside FE: mkdocs content writing</li> </ul>"},{"location":"phases/phase-3-enums-and-docs/#phase-dependencies","title":"Phase dependencies","text":"<ul> <li>Requires: Phase 1 (SignalBridgeABC must be documented), Phase 2 (EventProcessor enhancements must be documented)</li> </ul>"},{"location":"phases/phase-3-enums-and-docs/#complexity","title":"Complexity","text":"<ul> <li>Backend: S</li> <li>Frontend: M</li> <li>Infra: S</li> </ul>"},{"location":"phases/phase-3-enums-and-docs/#risks","title":"Risks","text":"<ul> <li>mkdocs-material + mkdocstrings-python version compatibility</li> <li>PyPI trusted publisher OIDC first-time setup may require manual intervention</li> </ul>"},{"location":"specs/backend/","title":"champi-signals v0.2.0 \u2014 Implementation Spec","text":""},{"location":"specs/backend/#overview","title":"Overview","text":"<p>This spec defines the internal implementation for champi-signals v0.2.0: module structure, class internals, signal dispatch model, async patterns, packaging automation, and CI/CD.</p> <p>Current version: 0.1.0 (stable, released on PyPI). Target version: 0.2.0 (this spec).</p>"},{"location":"specs/backend/#section-1-module-structure","title":"Section 1 \u2014 Module Structure","text":""},{"location":"specs/backend/#current-v010","title":"Current (v0.1.0)","text":"<pre><code>src/champi_signals/\n\u251c\u2500\u2500 __init__.py\n\u251c\u2500\u2500 managers.py      # SignalManagerABC, BaseSignalManager\n\u251c\u2500\u2500 processors.py    # EventProcessor\n\u251c\u2500\u2500 enums.py         # STTEventTypes, TTSEventTypes, EnumSetup\n\u2514\u2500\u2500 py.typed\n</code></pre>"},{"location":"specs/backend/#target-v020-additions-only","title":"Target (v0.2.0) \u2014 additions only","text":"<pre><code>src/champi_signals/\n\u251c\u2500\u2500 __init__.py          # add new exports\n\u251c\u2500\u2500 managers.py          # unchanged\n\u251c\u2500\u2500 processors.py        # add class-level decorator + context manager\n\u251c\u2500\u2500 enums.py             # add ImgUIEventTypes, make_event_types()\n\u251c\u2500\u2500 bridges.py           # NEW: SignalBridgeABC\n\u2514\u2500\u2500 py.typed\n</code></pre>"},{"location":"specs/backend/#section-2-signalbridgeabc-implementation-bridgespy","title":"Section 2 \u2014 <code>SignalBridgeABC</code> Implementation (bridges.py)","text":"<pre><code>from abc import ABC, abstractmethod\nfrom champi_signals.managers import BaseSignalManager\n\nclass SignalBridgeABC(ABC):\n    _manager: BaseSignalManager | None = None\n\n    @abstractmethod\n    def connect(self, signal_manager: BaseSignalManager) -&gt; None: ...\n\n    @abstractmethod\n    def disconnect(self) -&gt; None: ...\n\n    @abstractmethod\n    async def start(self) -&gt; None: ...\n\n    @abstractmethod\n    async def stop(self) -&gt; None: ...\n\n    @property\n    def is_connected(self) -&gt; bool:\n        return self._manager is not None\n</code></pre> <p>Testing: Unit tests in <code>tests/test_bridges.py</code> using a concrete stub that records calls.</p>"},{"location":"specs/backend/#section-3-enhanced-eventprocessor-processorspy","title":"Section 3 \u2014 Enhanced <code>EventProcessor</code> (processors.py)","text":""},{"location":"specs/backend/#31-class-level-decorator-emits_all_events","title":"3.1 Class-level decorator <code>emits_all_events</code>","text":"<p>Implementation strategy: 1. In <code>__init_subclass__</code> or via a class decorator, iterate <code>inspect.getmembers(cls, predicate=inspect.isfunction)</code> 2. Skip dunder methods, <code>Meta</code>, and private names 3. Apply <code>emits_event</code> to each remaining method using the class-level <code>data</code> param</p> <pre><code>@classmethod\ndef emits_all_events(cls, data: list[str] | None = None):\n    def decorator(klass):\n        for name, method in inspect.getmembers(klass, predicate=inspect.isfunction):\n            if not name.startswith('_') and name != 'Meta':\n                setattr(klass, name, cls.emits_event(data=data or [])(method))\n        return klass\n    return decorator\n</code></pre>"},{"location":"specs/backend/#32-context-manager-eventprocessorcontext","title":"3.2 Context manager <code>EventProcessor.context</code>","text":"<pre><code>from contextlib import asynccontextmanager\n\n@staticmethod\n@asynccontextmanager\nasync def context(signal_manager, event_type: str, data: dict | None = None):\n    signal_manager.emit(f\"{event_type}_START\", data=data)\n    try:\n        yield\n        signal_manager.emit(f\"{event_type}_FINISH\", data=data)\n    except Exception as e:\n        signal_manager.emit(f\"{event_type}_ERROR\", error=str(e), data=data)\n        raise\n</code></pre> <p>Testing: Add tests to <code>tests/test_processors.py</code> covering class decoration and context manager happy + error paths.</p>"},{"location":"specs/backend/#section-4-enum-additions-enumspy","title":"Section 4 \u2014 Enum Additions (enums.py)","text":""},{"location":"specs/backend/#41-imguieventtypes","title":"4.1 <code>ImgUIEventTypes</code>","text":"<p>Add alongside existing <code>STTEventTypes</code> and <code>TTSEventTypes</code>. Values start at 100 to avoid collision.</p>"},{"location":"specs/backend/#42-make_event_typesname-spec-factory","title":"4.2 <code>make_event_types(name, spec)</code> factory","text":"<p>Replacement for <code>EnumSetup.setup_service_enums</code> with a cleaner functional interface:</p> <pre><code>def make_event_types(name: str, spec: dict[str, list[str]]) -&gt; type[IntEnum]:\n    members: dict[str, int] = {}\n    counter = 0\n    for group, suffixes in spec.items():\n        for suffix in suffixes:\n            members[f\"{group}_{suffix}\"] = counter\n            counter += 1\n    return IntEnum(name, members)  # type: ignore[return-value]\n</code></pre> <p><code>EnumSetup</code> kept for backward compatibility; <code>make_event_types</code> is the recommended API going forward.</p>"},{"location":"specs/backend/#section-5-pypi-publishing-automation","title":"Section 5 \u2014 PyPI Publishing Automation","text":""},{"location":"specs/backend/#51-trusted-publisher-setup","title":"5.1 Trusted Publisher setup","text":"<ul> <li>Register champi-signals on PyPI as a trusted publisher (GitHub Actions OIDC)</li> <li>Workflow: <code>.github/workflows/release.yml</code> \u2014 triggers on <code>v*.*.*</code> tags</li> <li>Steps: <code>uv build</code> \u2192 <code>uv publish</code> (no API token stored in secrets)</li> </ul>"},{"location":"specs/backend/#52-version-bump-workflow","title":"5.2 Version bump workflow","text":"<ul> <li><code>cz bump</code> creates tag + updates CHANGELOG</li> <li>CI picks up tag, builds wheel + sdist, publishes to PyPI</li> </ul>"},{"location":"specs/backend/#section-6-documentation-site","title":"Section 6 \u2014 Documentation Site","text":""},{"location":"specs/backend/#stack","title":"Stack","text":"<ul> <li>mkdocs-material \u2265 9.5</li> <li>mkdocstrings-python (griffe backend)</li> <li>Deployed to GitHub Pages via <code>.github/workflows/docs.yml</code> on push to main</li> </ul>"},{"location":"specs/backend/#mkdocsyml-outline","title":"<code>mkdocs.yml</code> outline","text":"<pre><code>site_name: champi-signals\ntheme:\n  name: material\nplugins:\n  - mkdocstrings:\n      handlers:\n        python:\n          paths: [src]\nnav:\n  - Home: index.md\n  - API Reference: api/\n  - Integration Guide: integration.md\n  - Changelog: changelog.md\n</code></pre>"},{"location":"specs/backend/#section-7-test-coverage-requirements","title":"Section 7 \u2014 Test Coverage Requirements","text":"Module Current Target managers.py \u2705 covered maintain processors.py \u2705 covered add class-deco + context tests enums.py \u2705 covered add ImgUIEventTypes + make_event_types bridges.py \u274c new 100% lines <p>Minimum overall coverage: 90% (enforced in CI via <code>--cov-fail-under=90</code>).</p>"},{"location":"specs/backend/#section-8-cicd","title":"Section 8 \u2014 CI/CD","text":"<p>Existing: <code>ci.yml</code> (lint + test on push/PR).</p> <p>Add: - <code>release.yml</code>: builds + publishes to PyPI on version tag - <code>docs.yml</code>: builds + deploys mkdocs site on push to main</p>"},{"location":"specs/backend/#section-9-dependencies","title":"Section 9 \u2014 Dependencies","text":"<p>No new runtime dependencies. Dev dependencies to add:</p> <pre><code>[dependency-groups]\ndocs = [\n    \"mkdocs-material&gt;=9.5\",\n    \"mkdocstrings-python&gt;=1.8\",\n]\n</code></pre>"},{"location":"specs/backend/#section-10-milestones-phase-breakdown-input-for-plan-mvp","title":"Section 10 \u2014 Milestones / Phase Breakdown (input for plan-mvp)","text":"Phase Deliverable Phase 1: IPC Bridge Interface <code>SignalBridgeABC</code> in bridges.py, full tests, exported in init Phase 2: Enhanced EventProcessor class-level decorator + async context manager, full tests Phase 3: Enum Expansion <code>ImgUIEventTypes</code>, <code>make_event_types()</code>, full tests Phase 4: Packaging &amp; Docs PyPI trusted-publisher workflow, mkdocs site, docs CI"},{"location":"specs/frontend/","title":"champi-signals v0.2.0 \u2014 Public API Spec","text":""},{"location":"specs/frontend/#overview","title":"Overview","text":"<p>This spec defines the consumer-facing (public API) surface for champi-signals v0.2.0. It covers every importable symbol, decorator, enum, CLI command, and integration contract that downstream packages (champi-stt, champi-tts, champi-imgui, champi-ipc) depend on.</p> <p>Current version: 0.1.0 (stable, released on PyPI). Target version: 0.2.0 (this spec).</p>"},{"location":"specs/frontend/#section-1-existing-public-api-must-remain-backward-compatible","title":"Section 1 \u2014 Existing Public API (must remain backward-compatible)","text":"<pre><code>from champi_signals import (\n    BaseSignalManager,   # Extend to create custom signal manager\n    EventProcessor,      # Decorator for automatic event emission\n    STTEventTypes,       # Pre-defined STT event enum\n    TTSEventTypes,       # Pre-defined TTS event enum\n    EnumSetup,           # Utility for creating custom enums\n)\n</code></pre> <p>All symbols above are exported from <code>src/champi_signals/__init__.py</code>. No breaking changes permitted in v0.2.0.</p>"},{"location":"specs/frontend/#section-2-new-public-api-v020-additions","title":"Section 2 \u2014 New Public API (v0.2.0 additions)","text":""},{"location":"specs/frontend/#21-signalbridgeabc-ipc-bridge-interface","title":"2.1 <code>SignalBridgeABC</code> \u2014 IPC bridge interface","text":"<p>Purpose: Abstract interface that champi-ipc will implement to bridge blinker signals to shared memory. Defined in champi-signals so downstream code depends only on this library, not on champi-ipc directly.</p> <pre><code>from champi_signals import SignalBridgeABC\n\nclass SignalBridgeABC(ABC):\n    \"\"\"Abstract base for bridging blinker signals to an external transport.\"\"\"\n\n    @abstractmethod\n    def connect(self, signal_manager: BaseSignalManager) -&gt; None:\n        \"\"\"Attach this bridge to a signal manager.\"\"\"\n\n    @abstractmethod\n    def disconnect(self) -&gt; None:\n        \"\"\"Detach and release resources.\"\"\"\n\n    @abstractmethod\n    async def start(self) -&gt; None:\n        \"\"\"Start the bridge (background tasks, threads, etc.).\"\"\"\n\n    @abstractmethod\n    async def stop(self) -&gt; None:\n        \"\"\"Stop the bridge gracefully.\"\"\"\n</code></pre> <p>Export: <code>from champi_signals import SignalBridgeABC</code></p>"},{"location":"specs/frontend/#22-enhanced-eventprocessor-class-level-decoration-and-context-managers","title":"2.2 Enhanced <code>EventProcessor</code> \u2014 class-level decoration and context managers","text":"<p>New capability 1: Decorate an entire class so all public methods emit events without per-method decoration.</p> <pre><code>@EventProcessor.emits_all_events(data=['status'])\nclass ReadingService:\n    class Meta:\n        event_type = \"reading\"\n        signal_manager = TTSSignalManager()\n    ...\n</code></pre> <p>New capability 2: Context manager form for imperative code:</p> <pre><code>async with EventProcessor.context(signal_manager, \"reading\", data={'text': text}):\n    await synthesize(text)\n</code></pre> <p>Backward compatible: existing <code>@EventProcessor.emits_event(data=[...])</code> method decorator unchanged.</p>"},{"location":"specs/frontend/#23-imguieventtypes-mcp-event-enum","title":"2.3 <code>ImgUIEventTypes</code> \u2014 MCP event enum","text":"<p>Purpose: champi-imgui needs event types for MCP tool invocations. Add a pre-defined <code>ImgUIEventTypes</code> enum alongside <code>STTEventTypes</code> and <code>TTSEventTypes</code>.</p> <pre><code>from champi_signals import ImgUIEventTypes\n\nclass ImgUIEventTypes(IntEnum):\n    TOOL_CALL_START   = 100\n    TOOL_CALL_FINISH  = 101\n    TOOL_CALL_ERROR   = 102\n    CANVAS_UPDATE     = 110\n    WIDGET_CREATE     = 120\n    WIDGET_DELETE     = 121\n    RENDER_FRAME      = 130\n</code></pre> <p>Export: <code>from champi_signals import ImgUIEventTypes</code></p>"},{"location":"specs/frontend/#24-make_event_types-generic-factory","title":"2.4 <code>make_event_types()</code> \u2014 generic factory","text":"<p>Purpose: Programmatic creation of event-type enums from a declarative dict (generalises <code>EnumSetup</code>).</p> <pre><code>from champi_signals import make_event_types\n\nMyEvents = make_event_types(\"MyEvents\", {\n    \"PROCESS\": [\"START\", \"FINISH\", \"ERROR\"],\n    \"LOAD\":    [\"START\", \"FINISH\", \"ERROR\"],\n})\n# Generates: MyEvents.PROCESS_START=0, PROCESS_FINISH=1, PROCESS_ERROR=2, ...\n</code></pre> <p>Export: <code>from champi_signals import make_event_types</code></p>"},{"location":"specs/frontend/#section-3-cli","title":"Section 3 \u2014 CLI","text":"<p>No CLI is planned for champi-signals itself. CLI tooling for IPC belongs in champi-ipc.</p>"},{"location":"specs/frontend/#section-4-api-contract-for-downstream","title":"Section 4 \u2014 API Contract for Downstream","text":"Consumer Uses champi-stt <code>BaseSignalManager</code>, <code>EventProcessor</code>, <code>STTEventTypes</code> champi-tts <code>BaseSignalManager</code>, <code>EventProcessor</code>, <code>TTSEventTypes</code>, <code>EventProcessor.context</code> (new) champi-imgui <code>BaseSignalManager</code>, <code>ImgUIEventTypes</code> (new), <code>SignalBridgeABC</code> (new) champi-ipc <code>SignalBridgeABC</code> (new), <code>BaseSignalManager</code>"},{"location":"specs/frontend/#section-5-packaging-distribution","title":"Section 5 \u2014 Packaging &amp; Distribution","text":"<ul> <li>Package name: <code>champi-signals</code> (unchanged)</li> <li>PyPI: published via GitHub Actions trusted-publisher on version tag</li> <li>Minimum Python: 3.12</li> <li>Extras: none planned</li> </ul>"},{"location":"specs/frontend/#section-6-documentation","title":"Section 6 \u2014 Documentation","text":"<ul> <li>mkdocs-material site auto-built from docstrings (mkdocstrings-python)</li> <li>Published to GitHub Pages on every release tag</li> <li>Sections: Getting Started, API Reference, Integration Guide, Changelog</li> </ul>"}]}